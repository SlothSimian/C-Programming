//wap to print nat no. from 10 to 20 when it is initialised as 0 using while loop
#include<stdio.h>
int main()
{
    int i = 0;
    while (i<=20)
    {
        if (i>=10){
        printf("the value of i is %d\n",i);
        }
        i++;
    }
    
    return 0;
}