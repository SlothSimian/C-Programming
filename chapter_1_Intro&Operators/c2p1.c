//Using preinputted values, wap to calculate area of rectangle
#include<stdio.h>
int main()
{
    int length, breadth;
    length = 20;
    breadth = 10;
    printf("The area of Rectangle is %d units",length*breadth);
    return 0;
}